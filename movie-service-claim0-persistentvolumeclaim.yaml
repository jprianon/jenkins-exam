apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: movie-service-claim0
  name: movie-service-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
