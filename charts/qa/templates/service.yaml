apiVersion: v1
kind: Service
metadata:
  name: {{ include "qa.fullname" . }}
  labels:
    {{- include "qa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qa.selectorLabels" . | nindent 4 }}
