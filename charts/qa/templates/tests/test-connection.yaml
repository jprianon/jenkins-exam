apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qa.fullname" . }}-test-connection"
  labels:
    {{- include "qa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
